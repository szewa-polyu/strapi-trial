{
  "routes": [
    {
      "method": "GET",
      "path": "/shows",
      "handler": "show.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/shows/count",
      "handler": "show.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/shows/:id",
      "handler": "show.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/shows",
      "handler": "show.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/shows/:id",
      "handler": "show.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/shows/:id",
      "handler": "show.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
